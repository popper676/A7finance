# OpenAI API Key Configuration
# Copy this file to .env and add your actual API key

# Option 1: Use VITE_ prefix (recommended for Vite projects)
VITE_OPENAI_API_KEY=sk-your-openai-api-key-here

# Option 2: Use OPENAIAPI (also supported)
OPENAIAPI=sk-your-openai-api-key-here

# Note: The app will check both variables in this order:
# 1. process.env.OPENAIAPI
# 2. import.meta.env.OPENAIAPI
# 3. import.meta.env.VITE_OPENAI_API_KEY

# Currency Exchange Rate API Key (Optional)
# If not provided, the app will use exchangerate.host (free, no API key required)
# To use exchangerate-api.com instead, get a free API key from https://www.exchangerate-api.com/
# VITE_EXCHANGE_RATE_API_KEY=your-exchange-rate-api-key-here

